{
  "name": "routing-oracle",
  "version": "1.0.0",
  "description": "Multi-model routing validator — tests delegate_to_model, weather_report, and consensus_vote MCP tools",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "zod": "^3.24.0"
  }
}
